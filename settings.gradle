pluginManagement {
    plugins {
        id 'org.jetbrains.kotlin.jvm' version "1.5.21"
    }
}

rootProject.name = 'knio2'
include 'lib', 'examples'

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}
